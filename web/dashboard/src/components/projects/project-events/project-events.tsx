import Events from '@/components/components/component/version/tabs/events';
import React from 'react';


export const ProjectEvents = ({ events }) => {

  return (<Events events={events.data}></Events>);
};

