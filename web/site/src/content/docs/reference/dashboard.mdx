---
title: Dashboard
description:  Unlock the power of your component library by seamlessly publishing it to the Ethereal Nexus dashboard.
---
import { Code } from '@astrojs/starlight/components';
export const npmInstall = `npm install`;
export const npmRunStart = `npm run start`;

#### 1. Running the Dashboard

On the folder ethereal-nexus-dashboard run the following command:

#### 2. Install dependencies
<Code code={npmInstall} lang="shell" />
#### 3. Run the server
<Code code={npmRunStart} lang="shell" />
#### 4. Open the browser
Open your browser and go to [http://localhost:3000](http://localhost:3000)
